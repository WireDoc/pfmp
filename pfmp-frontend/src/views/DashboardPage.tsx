// Dashboard skeleton placeholder (Wave 1). Real data & sections reintroduced in later waves.

export default function DashboardPage() {
  // Placeholder; later will use hooks + queries
  return (
    <div style={{ padding: 24 }}>
      <h1>Dashboard</h1>
  <p>Wave 1 routing skeleton active. Dashboard content will be reintroduced in Waves 4+.</p>
    </div>
  );
}
