{
  "name": "pfmp-mcp-bridge",
  "version": "0.1.0",
  "description": "STDIO to HTTP/SSE bridge for the PFMP MCP PostgreSQL server",
  "type": "module",
  "bin": {
    "pfmp-mcp-bridge": "./bridge.mjs"
  },
  "scripts": {
    "start": "node ./bridge.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0",
  "eventsource": "^2.0.2"
  }
}
